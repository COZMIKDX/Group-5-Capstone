Necessary installations (not including those already pre-packaged in this list): Python, flask, SQLAlchemy, [].

For local hosting: PGAdmin (You'll need to set up the server using what running the app.py tells you (probably localhost, port x, etc)
